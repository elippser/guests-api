/**
 * Punto de entrada en Vercel (serverless). En local se usa `npm run dev` / `npm start`
 * con `src/index.ts`, que hace listen() en un puerto.
 */
import app from "../src/server";

export default app;
